---
title: "Components"
order: 3
tags : ['top_menu']
showCategoryMenu : true
---
<p>Components are reusable parts of the user interface that have been made to support a variety of applications.</p>
<p>Any components with interactive capabilities can also be navigated using the keyboard, for accessibility purposes.</p>
<p>You can use individual components in many different <a href="../patterns">patterns</a> and contexts. For example, you can use the text input component to ask for an ==email address==, a ==telephone number== or someone’s ==name==.</p>
<p>Use the links on this page to navigated to the component.</p>

<div class="govcy-container">
    <div class="row">
    {%- for pag in collections[category] -%}
        {#- Checks for same locale -#}
        {% if locale  == pag.data.locale -%}
            {#- Checks if we want to show the index page -#}
            {% if (pag.fileSlug == category and showIndexInCategoryMenu) or pag.fileSlug != category -%}
                <div class="govcy-col-4 govcy-mb-3">
                   <a class="app-card" href="{{ pag.url | url }}">
                         <h3>{{ pag.data.title }}</h3>
                        {% if pag.data.description %}<p class="flex-fill">{{ pag.data.description }}</p>{% endif %}
                    </a>
                </div>
            {%- endif %}
        {%- endif %}
    {%- endfor -%}
    </div>
</div> 